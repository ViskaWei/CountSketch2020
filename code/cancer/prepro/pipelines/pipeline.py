import numpy as np

from cancer.prepro.dataset import Dataset




    def run_step_loading()

    def get_spec

    def run_step_multiple(idx,images, ISSMTH, SMTH, TEST):
        
        images.get_data(idx, ISSMTH, SMTH)
        return images.data1D[idx].T.dot(images.data1D[idx])

            if self.isSmooth: 
